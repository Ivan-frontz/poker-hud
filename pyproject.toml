[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "poker-hud"
version = "0.1.0"
description = "HUD casero para PokerStars: parser de hand history, motor de stats y overlay en mesa"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
